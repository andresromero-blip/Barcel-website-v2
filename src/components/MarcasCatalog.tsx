// Deprecado: este componente era el "hub" /marcas (listado de todas las
// marcas en una sola página). Se retiró a favor de páginas individuales
// por marca — ver src/app/marcas/[slug]/page.tsx y src/components/BrandPage.tsx.
// El sandbox de este entorno no permite borrar archivos creados en una
// llamada anterior, así que este archivo queda vacío/sin usar en vez de
// eliminado; no se importa desde ningún lado del sitio.
export {};
